const requestsRouter = require('./requestsRouter');
const lockersRouter = require('./lockersRouter');
const accountsRouter = require('./accountsRouter');

module.exports = {
    requestsRouter,
    lockersRouter,
    accountsRouter
}